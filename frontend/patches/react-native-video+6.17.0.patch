diff --git a/node_modules/react-native-video/android/src/main/java/com/brentvatne/exoplayer/ExoPlayerView.kt b/node_modules/react-native-video/android/src/main/java/com/brentvatne/exoplayer/ExoPlayerView.kt
index bb945fe..5be70b2 100644
--- a/node_modules/react-native-video/android/src/main/java/com/brentvatne/exoplayer/ExoPlayerView.kt
+++ b/node_modules/react-native-video/android/src/main/java/com/brentvatne/exoplayer/ExoPlayerView.kt
@@ -214,8 +214,8 @@ class ExoPlayerView @JvmOverloads constructor(context: Context, attrs: Attribute
         val isLive = player.isCurrentMediaItemLive
         val seekable = player.isCurrentMediaItemSeekable
 
-        // Show/hide badge
-        liveBadge.visibility = if (isLive) View.VISIBLE else View.GONE
+        // Always hide live badge - HLS is used for VOD content
+        liveBadge.visibility = View.GONE
 
         // Disable/enable scrubbing based on seekable
         val timeBar = playerView.findViewById<DefaultTimeBar?>(androidx.media3.ui.R.id.exo_progress)
diff --git a/node_modules/react-native-video/android/src/main/java/com/brentvatne/exoplayer/ReactExoplayerView.java b/node_modules/react-native-video/android/src/main/java/com/brentvatne/exoplayer/ReactExoplayerView.java
index f175dec..2c05cad 100644
--- a/node_modules/react-native-video/android/src/main/java/com/brentvatne/exoplayer/ReactExoplayerView.java
+++ b/node_modules/react-native-video/android/src/main/java/com/brentvatne/exoplayer/ReactExoplayerView.java
@@ -17,6 +17,8 @@ import android.content.ComponentName;
 import android.content.Context;
 import android.content.Intent;
 import android.content.ServiceConnection;
+import android.content.res.Configuration;
+import android.app.UiModeManager;
 import android.media.AudioManager;
 import android.net.Uri;
 import android.os.Build;
@@ -709,8 +711,14 @@ public class ReactExoplayerView extends FrameLayout implements
     private void initializePlayerCore(ReactExoplayerView self) {
         ExoTrackSelection.Factory videoTrackSelectionFactory = new AdaptiveTrackSelection.Factory();
         self.trackSelector = new DefaultTrackSelector(getContext(), videoTrackSelectionFactory);
+
+        // Enable tunneled playback on TV devices for smoother video with hardware frame rate matching
+        UiModeManager uiModeManager = (UiModeManager) getContext().getSystemService(Context.UI_MODE_SERVICE);
+        boolean isTv = uiModeManager != null && uiModeManager.getCurrentModeType() == Configuration.UI_MODE_TYPE_TELEVISION;
+
         self.trackSelector.setParameters(trackSelector.buildUponParameters()
-                .setMaxVideoBitrate(maxBitRate == 0 ? Integer.MAX_VALUE : maxBitRate));
+                .setMaxVideoBitrate(maxBitRate == 0 ? Integer.MAX_VALUE : maxBitRate)
+                .setTunnelingEnabled(isTv));
 
         DefaultAllocator allocator = new DefaultAllocator(true, C.DEFAULT_BUFFER_SEGMENT_SIZE);
         RNVLoadControl loadControl = new RNVLoadControl(
diff --git a/node_modules/react-native-video/ios/Video/Features/RCTPictureInPicture.swift b/node_modules/react-native-video/ios/Video/Features/RCTPictureInPicture.swift
index 21d321a..6907356 100644
--- a/node_modules/react-native-video/ios/Video/Features/RCTPictureInPicture.swift
+++ b/node_modules/react-native-video/ios/Video/Features/RCTPictureInPicture.swift
@@ -2,9 +2,9 @@ import AVFoundation
 import AVKit
 import Foundation
 import MediaAccessibility
-import React
 
 #if os(iOS)
+    import React
     class RCTPictureInPicture: NSObject, AVPictureInPictureControllerDelegate {
         public private(set) var _pipController: AVPictureInPictureController?
         private var _onPictureInPictureEnter: (() -> Void)?
diff --git a/node_modules/react-native-video/ios/Video/RCTVideo.swift b/node_modules/react-native-video/ios/Video/RCTVideo.swift
index ac4232c..74e62e1 100644
--- a/node_modules/react-native-video/ios/Video/RCTVideo.swift
+++ b/node_modules/react-native-video/ios/Video/RCTVideo.swift
@@ -302,6 +302,10 @@ class RCTVideo: UIView, RCTVideoPlayerViewControllerDelegate, RCTPlayerObserverH
             _pip = nil
         #endif
 
+        #if os(tvOS)
+            clearDisplayCriteria()
+        #endif
+
         ReactNativeVideoManager.shared.unregisterView(newInstance: self)
         AudioSessionManager.shared.unregisterView(view: self)
     }
@@ -628,10 +632,47 @@ class RCTVideo: UIView, RCTVideoPlayerViewControllerDelegate, RCTPlayerObserverH
                 _imaAdsManager.setUpAdsLoader()
             }
         #endif
+
+        #if os(tvOS)
+            // Set AVDisplayManager criteria for HDR content on tvOS
+            // This enables automatic display mode switching for HDR/Dolby Vision content
+            if #available(tvOS 11.2, *) {
+                Task {
+                    await self.updateDisplayCriteria(for: playerItem.asset)
+                }
+            }
+        #endif
+
         isSetSourceOngoing = false
         applyNextSource()
     }
 
+    #if os(tvOS)
+    @available(tvOS 11.2, *)
+    private func updateDisplayCriteria(for asset: AVAsset) async {
+        do {
+            let criteria = try await asset.load(.preferredDisplayCriteria)
+            await MainActor.run {
+                if let window = self.window {
+                    window.avDisplayManager.preferredDisplayCriteria = criteria
+                    DebugLog("[RCTVideo] Set display criteria for HDR content")
+                }
+            }
+        } catch {
+            DebugLog("[RCTVideo] Could not load display criteria: \(error)")
+        }
+    }
+
+    func clearDisplayCriteria() {
+        if #available(tvOS 11.2, *) {
+            if let window = self.window {
+                window.avDisplayManager.preferredDisplayCriteria = nil
+                DebugLog("[RCTVideo] Cleared display criteria")
+            }
+        }
+    }
+    #endif
+
     @objc
     func setSrc(_ source: NSDictionary!) {
         if self.isSetSourceOngoing || self.nextSource != nil {
